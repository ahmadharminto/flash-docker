version: '2'

services:
  web_app:
    build: .
    command: >
      gunicorn -b 0.0.0.0:9090
      --access-logfile -
      --reload
      "web_app.app:create_app()"
    environment:
      PYTHONUNBUFFERED: 'true'
    volumes:
      - '.:/web_app'
    ports:
      - '9090:9090'